import React from 'react';
import Route from './components/Route';
import { Text, Alert, Vibration, Platform, View, Button, } from 'react-native';


export default class App extends React.Component {

  render() {
      return (
       <Route/>
    );
  }
}